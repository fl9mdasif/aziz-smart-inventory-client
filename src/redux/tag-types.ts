export enum tagTypes {
  users = "users",
  orders = "orders",
  products = "products",
  categories = "categories",
  activity = "activity",
}

export const tagTypesList = [
  tagTypes.users,
  tagTypes.orders,
  tagTypes.products,
  tagTypes.categories,
  tagTypes.activity,
];
